export interface Node {
  id: string;
  price: number;
}

export interface Position {
  x: number;
  y: number;
}

export interface FormData {
  S: number;
  T: number;
  sigma: number;
  n: number;
}